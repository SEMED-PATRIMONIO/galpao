# Aplicativo OLITEMQ (Quiz/Formulário)
server {
    listen 80;
    server_name olitemq.paiva.api.br;

    location / {
        proxy_pass http://localhost:3023;
        
        # --- LINHAS ESSENCIAIS PARA IP REAL E WEBSOCKETS ---
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # --------------------------------------------------
    }
}

# Aplicativo Dashboard do OLITEMQ
server {
    listen 80;
    server_name resultadolitemq.paiva.api.br; # Ajuste este domínio se desejar outro nome

    location / {
        proxy_pass http://localhost:3024;

        # --- LINHAS ESSENCIAIS PARA IP REAL E WEBSOCKETS ---
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # --------------------------------------------------
    }
}